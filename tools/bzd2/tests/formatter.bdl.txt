/*
 This is a multi-line comment
*/

// Contracts
const int32 defaultConstant [min = -1 , max = 35 ];

int32<Int, List<T<A, B, C<45>>>> defaultConstant;

interface MyFy {
                 MyType var;

/*
          A nested comment
         */

        const MyType<T> varConst [ always ];
 MyType varInitialized = 42;    
}

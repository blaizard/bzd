const Path = require("path");
const Webpack = require("./nodejs/webpack.js");

const config = {
	entries: {
		{entries}
	},
	publicPath: "{public_path}",
	alias: {
		{aliases}
	},
	templates: {
		{templates}
	}
};

module.exports = Webpack.generate(config);

load("@bzd_rules_nodejs//nodejs:defs.bzl", "bzd_nodejs_requirements_compile")

# bazel run @bzd_sanitizer//nodejs:requirements
bzd_nodejs_requirements_compile(
    name = "requirements",
    srcs = [
        "requirements.in",
    ],
    output = "requirements.json",
)

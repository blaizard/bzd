load("//:defs.bzl", "bdl_library")

bdl_library(
    name = "a",
    srcs = [
        "a.bdl",
    ],
)

bdl_library(
    name = "preset",
    srcs = ["preset.bdl"],
    presets = ["fixture/config.json"],
)

load("//:defs.bzl", "bdl_library")
load("//tests/helper:defs.bzl", "bzl_system_test")

bdl_library(
    name = "a",
    srcs = [
        "a.bdl",
    ],
)

bzl_system_test(
    name = "a_test",
    expected_json = ":expected_a.json",
    deps = [
        ":a",
    ],
)

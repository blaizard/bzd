
namespace bzd;


preset cfg from "external/bzd_bdl+/tests/fixture/config.json";
struct Server {
	host = String;
	port = Integer;
	
}

composition MyService {
	server = Server(host = cfg.host, port = cfg.port);
	
}



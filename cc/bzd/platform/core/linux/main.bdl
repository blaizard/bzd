use "cc/bzd/platform/core/linux/linux.bdl"


composition {
	defaultStackSize = bzd.platform.StackSize(100);
	core1 = bzd.platform.linux.Core(name = 32.3, priority = 100);
	core2 = bzd.platform.linux.Core(priority = 20, stackSize = defaultStackSize);
	executor = bzd.platform.linux.Executor(core1, core2);
	
}

method temp(var = const Integer) -> Integer;
composition {
	temp(defaultStackSize);
	
}



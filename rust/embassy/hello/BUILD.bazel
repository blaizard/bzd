load("@rules_rust//rust:defs.bzl", "rust_library")
load("//rust:defs.bzl", "rust_test")

rust_library(
    name = "hello",
    srcs = [
        "lib.rs",
    ],
    tags = ["manual"],
    deps = ["//rust/targets:embassy"],
)

rust_test(
    name = "hello_test",
    crate = ":hello",
    tags = ["manual"],
    target = ["//rust/targets:embassy"],
)

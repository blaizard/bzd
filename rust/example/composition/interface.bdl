
namespace bzd.components;


component Person {
config:
	username = String;
	age = Integer [min(0)];
	
interface:
	method print_info();
	
}

component Child : Person {
}

component Parent : Person {
config:
	children = list(Child);
	
}



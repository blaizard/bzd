# MCP

## Test

Run `modelcontextprotocol/inspector`:

```bash
nix shell nixpkgs#nodejs -c npx @modelcontextprotocol/inspector
```

Use:

```
Transport Type: Streamable HTTP
URL: http://localhost:8081/x/nodes/@mcp/compute
```

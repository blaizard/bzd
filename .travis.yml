sudo: false
language: cpp
compiler:
  - gcc
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-7
install:
  - "./tools/bazel.build/setup_bazel.sh"
script:
  - "./quality_gate.sh"

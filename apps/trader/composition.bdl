use "cc/bzd/platform/interfaces/tcp.bdl"

namespace trader;


component Trader {
interface:
	method run();
	
config:
	client = bzd.platform.network.tcp.Client;
	
}

composition {
	trader = Trader(client = network.tcp.client);
	trader.run();
	
}



use "cc/bzd/platform/interfaces/tcp.bdl"

namespace trader;


component Trader : ComponentBase {
interface:
	method run();
	
config:
	client = bzd.platform.network.tcp.Client;
	
}

composition {
	trader = Trader(client = target.network.tcp.client);
	trader.run();
	
}



import ExceptionFactory from "#bzd/nodejs/core/exception.mjs";

const Exception = ExceptionFactory("artifacts", "nodes", "external", "influxdb");

export default async function external(uid, key, count, after, before) {}

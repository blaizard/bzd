
namespace example;


component Hello {
interface:
	method run();
	input = Float;
	
composition:
	this.run();
	
}

composition {
	hello = Hello();
	hello.run() [executor(executor)];
	world = Hello();
	connect(hello.input, world.input);
	
}

/**
 * This empty project is only to demonstrate the compilation capabilities of the toolchain.
 * Used mostly as a tool to port a new toolchain. 
 */

int main()
{
	while (1); // Used to create a non-empty main
	return 0;
}

namespace bzd.manifest;

using NextType = Integer;
enum Test
{
	// First test value.
	FIRST
,	SECOND
}

struct MyType{
	test = Test;
	type1 = NextType;
	
}

interface MyInterface{
	method start(a = NextType) -> Test;
	// Magic number for demo purpose
	magic = Integer(42) [min = 5, max = 100];
	// Read only value
	readOnly = const Float(-3.14);
	
}




namespace example;


// Emit integer continuously.
component Emitter : ComponentBase {
interface:
	method generate();
	send = Integer;
	
composition:
	this.generate();
	
}

// Poll and receive data.
component Poller : ComponentBase {
interface:
	method poll();
	receive = const Integer;
	
config:
	id = const Integer;
	
}


